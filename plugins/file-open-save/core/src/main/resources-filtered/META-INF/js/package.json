{
  "name": "${project.artifactId}",
  "version": "${project.version}",
  "description": "${project.description}",
  "main": "app.module.js",
  "dependencies": {
    "angular": "${angular.version}",
    "angular-ui-router": "${angular-ui-router.version}",
    "angular-i18n": "${angular-i18n.version}",
    "require-css": "${require-css.version}",
    "requirejs-text": "${requirejs-text.version}",
    "dojo": "${dojo.version}"
  },
  "config": {
    "pentaho/modules": {
      "IPenFileService": {"base": null, "isAbstract": true},
      "${project.artifactId}_${project.version}/services/vfs.service": {"type": "IPenFileService"},
      "${project.artifactId}_${project.version}/services/local.service": {"type": "IPenFileService"},
      "${project.artifactId}_${project.version}/services/repository.service": {"type": "IPenFileService"}
    }
  }
}
